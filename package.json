{
  "name": "pageify",
  "version": "1.0.0",
  "description": "Generate a page.js client side router.",
  "main": "transform.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "pagejsify": "./bin/pagejsify.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify-transform-tools": "^1.3.0"
  }
}
